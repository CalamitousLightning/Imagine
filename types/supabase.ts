// Placeholder. Once schema.sql + policies.sql + storage.sql are applied to your
// actual Supabase project, regenerate real types with:
//   npx supabase gen types typescript --project-id <your-project-id> --schema public > types/supabase.ts
export type Database = any;
